export enum Pages {
    HOME,
    SKILLS,
    ABOUT,
    PROJECTS,
    SEARCHING,
    SKILLS_WEB,
    SKILLS_DESKTOP
}

export enum Links {
    GITHUB,
    LINKEDIN
}

export enum SearchAction {
    IDLE,
    TARGET, 
    CLEAR,
    WALL,
    SOURCE,
    WEIGHT
}

export enum CellState {
    SOURCE,
    EMPTY,
    WALL,
    TARGET,
    WEIGHT
}
