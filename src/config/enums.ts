export enum Pages {
    HOME,
    SKILLS,
    ABOUT,
    PROJECTS,
    SEARCHING
}

export enum Links {
    GITHUB
}

export enum SearchAction {
    IDLE,
    TARGET, 
    CLEAR,
    WALL,
    SOURCE,
    WEIGHT
}

export enum Colour {
    LIGHT = 'bg-blue-300',
    LIGHT_HOVER = 'hover:bg-blue-400',
    DARK = 'dark:bg-slate-600',
    DARK_HOVER = 'hover:dark:bg-blue-200',
    SELECTED = 'bg-red-300'
}
