export enum Pages {
    HOME,
    SKILLS,
    ABOUT,
    PROJECTS,
    SEARCHING
}

export enum Links {
    GITHUB
}

export enum SearchAction {
    IDLE,
    TARGET, 
    CLEAR,
    WALL,
    SOURCE,
    WEIGHT
}
